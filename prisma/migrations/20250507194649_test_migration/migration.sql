BEGIN TRY

BEGIN TRAN;

-- DropIndex
ALTER TABLE [dbo].[DoctorSchedule] DROP CONSTRAINT [DoctorSchedule_doctorId_date_key];

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
